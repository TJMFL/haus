export async function fetchWeather(location: string) {
    // Replace with OpenWeatherMap API call
    return `Current weather at ${location}: 65°F, partly cloudy, 10% chance of rain (simulated on March 25, 2025).`;
  }
  
  export async function fetchCrimeData(location: string) {
    // Replace with real crime API or web search
    return `Crime stats for ${location}: Moderate risk, recent reports of theft near downtown (simulated).`;
  }